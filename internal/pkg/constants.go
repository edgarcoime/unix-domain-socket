package pkg

const (
	// TODO: remove when client code is also done
	DEFAULT_SOCKET_FILE = "/tmp/dss.sock"

	DEFAULT_MAX_CLIENTS = 10
	SERVER_TYPE         = "tcp"
	DEFAULT_SERVER_ADDR = "0.0.0.0"
	BYTE_BUFFER         = 1024
)
